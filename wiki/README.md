# Wiki

`wiki/` is the LLM-maintained knowledge layer. It contains source summaries, concept pages, practice pages, tool pages, decisions, syntheses, questions, and lint reports.

Start with `index.md`, then follow wikilinks. Update `log.md` after meaningful maintenance work.
